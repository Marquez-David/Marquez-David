---
import { BG_COLOR, BORDER_COLOR } from "../../utils/colors";
---

<a
  href={Astro.props.href}
  class={`rounded-full ${BG_COLOR} border ${BORDER_COLOR} flex justify-center items-center gap-x-2 py-2 px-8 hover:scale-105 transition`}
  target="_blank"
  rel="noopener noreferrer"
>
  <slot />
</a>
